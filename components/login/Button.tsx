import React from 'react';
import Button from '@material-ui/core/Button';

const CompoButton = (props) => {
  return (
    <>
      <Button>props.title</Button>
    </>
  );
};

export default Button;
